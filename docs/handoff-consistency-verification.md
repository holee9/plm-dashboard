# 핸드오프 일관성 수정·배포 검증

검증일: 2026-09-11. 기준: 40b0ef4 기반 #57/#58/#60/#67 수정.
대상: 로컬 라이브 서비스 http://127.0.0.1:8080/ (운영 프록시 API 사용).
원격 plm-dash.work 정적 배포와 GitHub main 반영 여부는 로컬 배포와 구분한다.

## 사전 사이드이펙트 점검과 보완

- 건강도 기본값 제거 시 Projects/Timeline의 삼항 색상 식이 미평가를 빨간색으로 처리할 수 있어 공통 healthColor를 사용.
- 전체 미평가 또는 프로젝트 0건일 때 Overview가 정상이라고 단정하지 않도록 집계·문구 보완. 기존 실제 지연/위험 신호 유지.
- 날짜 null이 간트 좌표 및 Version 번다운에 전달돼 오류를 일으킬 수 있어 날짜 유효성·기간 상태를 검증.
- 개별 WP에 시작일/마감일이 하나씩만 있는 경우 가상 프로젝트 기간이 생기지 않도록 유효한 기간 원천 존재 여부 확인.
- 마일스톤이 프로젝트 기간에서 분리돼도 날짜 축과 표식이 사라지지 않도록 축 집계에 포함.
- 잘못된 새 데이터가 이전 스냅샷을 부분 덮어쓰기하지 않도록 사전 준비 후 적용. 수신 시각은 적용 성공 후에만 기록.
- 최초 오류 및 갱신 실패의 사이드바·표시등을 정상 연동과 구분.
- 모바일에서 추가된 미평가 집계와 마감 카드가 겹치는 것을 발견해 세로 배치로 보완.

## 자동 검증

`node --test tests/handoff-consistency.test.cjs`

기존 코드에서 최초 9개 테스트 중 7개 실패를 확인한 후 수정했다. Version/초기 오류 등의
추가 회귀를 포함한 12개 테스트가 통과했다.

- 누락/유효 건강도, 모든 미평가, 빈 프로젝트, 실제 위험 유지
- 날짜 누락·한쪽 날짜·역전·잘못된 달력 날짜·동일 날짜·마일스톤 전용 프로젝트
- 서로 다른 WP의 한쪽 날짜를 가상 기간으로 합치지 않음
- 날짜 없는/역전된 Version 번다운 안전성
- displayId 우선 및 numeric id 링크 fallback
- 성공/화면 전환/다음 날/실패/재성공의 수신 시각
- 비정상 데이터셋 실패 시 이전 데이터/시각 보존
- 6개 뷰의 누락 데이터 렌더링

전체 JS `node --check`, `git diff --check` 통과.

## 실브라우저 검증

8081 격리 서버에서 같은 운영 API로 사전 검증했다.
- 실데이터 576 WP, 37 프로젝트: 미평가 37, 마감 초과 55의 별도 표시.
- 전체 Timeline 37행 중 유효한 기간 19개만 막대로 표시. NaN/Infinity 좌표 0.
- TnS 범위: 등록된 일정 없음. BLUE HD 범위: BH 표시 ID 링크 및 마일스톤 5개 유지.
- 화면 전환 후 수신 시각 유지. 6개 화면에서 연동/렌더링 오류 안내 없음.
- 1440px Resources: 폭 715.06:446.92, 높이 312/432px로 좌우 일치.
- Projects KPI 편집 취소 버튼 hit-test 정상, 실제 취소 성공.
- Chrome 확장 프로그램에서 발생하는 기존 `Object` 콘솔 오류는 별도 존재하므로 전체 콘솔 오류 0이라고 주장하지 않는다.

## 배포 후 결과

2026-09-11 15:32 KST, 구현 커밋 `de27faf`를 로컬 main에 fast-forward하여 8080 서비스에 반영했다.

- 배포된 HTML/JS/CSS 23개 파일의 SHA-256이 검증한 소스와 모두 일치.
- `/__plm_health`, `/op/users/me` HTTP 200. 운영 API 데이터 576 WP / 37 프로젝트 / 15명 수신 완료.
- 배포 소스의 회귀 테스트 12/12 통과.
- 실제 8080 브라우저에서 Overview/Projects/Resources/Board/Timeline/Risks 6개 화면 정상 렌더링. 화면 전환 중 수신 시각 15:32 유지.
- Overview 미평가 37과 마감 초과 55 표시 및 기존 위험 상태 유지.
- Timeline BLUE HD의 BH-1 → numeric WP 520 등 링크와 마일스톤 5개 확인. TnS는 가상 기간 없이 등록된 일정 없음 표시.
- 사전 검증에서 모바일 390px 겹침 수정 및 재확인, 실제 수동 갱신 중 이전 데이터 유지·성공 후 시각 갱신 확인.
- 변경 전 파일은 로컬 캐시의 `plm-dashboard-deploy/before-de27faf-20260911-153216.tar.gz`에 보관.

검증 완료 범위는 로컬 8080 서비스다. 이 절은 로컬 사전 검증 당시 기록이다. 이후 운영 반영 결과는 아래 추가 기록을 참조한다.

## 범위 밖 백로그

#59/#61~#66(고정 증감·기간 버튼·목록 제한·접근성·모바일 툴바·부하 신뢰도·가독성)은 별도 이슈로 유지.

## 운영 배포 후 추가 확인

2026-09-11 사용자 raspi5p에서 원본 main `d4266fc`를 fast-forward pull했다.
`http://plm-dash.work/` 브라우저 강력 새로고침 후 미평가 37, WP 576, TnS 일정 없음,
BLUE HD BH-1→WP 520 링크와 마일스톤 5개를 확인했다. 6개 화면 전환 중 수신 시각 유지.
변경된 런타임 파일 7개의 원격 응답 SHA-256이 로컬 소스와 일치했다.
추가 partial endpoint 회귀 수정까지 포함한 테스트 13개가 통과했다.
#57/#58/#60/#67은 검증 근거와 함께 종결했다. 컨테이너 재시작은 필요하지 않았다.
